# How to work with a project:

- clone project
- setup .env file with 
  	```
      PORT = numberOFPort
      MONGODB_URI = mongodb://localhost/yourdb-name
    ```
      
- npm install in terminal
- npm run dev